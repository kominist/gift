(function() {
  var __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

  define(["marionette", "validate"], (function(Marionette, Validate) {
    var GiftModel;
    return GiftModel = (function(_super) {
      __extends(GiftModel, _super);

      function GiftModel() {
        return GiftModel.__super__.constructor.apply(this, arguments);
      }

      GiftModel.prototype.isCurrentUser = function(user) {
        if (this.get("status") === "initialized") {
          if (user === this.get("giver").name) {
            this.set("cancelable", true);
          } else {
            this.set("cancelable", false);
          }
          if (user === this.get("getter").name) {
            this.set("refusable", true);
          } else {
            this.set("refusable", false);
          }
        }
        return true;
      };

      return GiftModel;

    })(Backbone.Model);
  }));

}).call(this);
