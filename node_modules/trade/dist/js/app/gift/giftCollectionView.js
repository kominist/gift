(function() {
  var __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

  define(["marionette", "gift/giftCollection", "gift/gift", "gift/zerogift", "user/userModel"], (function(Marionette, GiftCollection, Gift, ZeroGiftView, UserModel) {
    var GiftCollectionView;
    return GiftCollectionView = (function(_super) {
      __extends(GiftCollectionView, _super);

      function GiftCollectionView() {
        return GiftCollectionView.__super__.constructor.apply(this, arguments);
      }

      GiftCollectionView.prototype.collection = GiftCollection;

      GiftCollectionView.prototype.itemView = Gift;

      GiftCollectionView.prototype.emptyView = ZeroGiftView;

      GiftCollectionView.prototype.setUser = function(currentUser) {
        return this.user = currentUser;
      };

      GiftCollectionView.prototype.onBeforeRender = function() {
        var model, _i, _len, _ref, _results;
        _ref = this.collection.models;
        _results = [];
        for (_i = 0, _len = _ref.length; _i < _len; _i++) {
          model = _ref[_i];
          _results.push(model.isCurrentUser(this.user));
        }
        return _results;
      };

      return GiftCollectionView;

    })(Marionette.CollectionView);
  }));

}).call(this);
