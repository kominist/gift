(function() {
  var __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

  define(["marionette", "gift/giftModel"], (function(Marionette, GiftModel) {
    var GiftSearch;
    return GiftSearch = (function(_super) {
      __extends(GiftSearch, _super);

      function GiftSearch() {
        return GiftSearch.__super__.constructor.apply(this, arguments);
      }

      GiftSearch.prototype.template = "#gift-search";

      GiftSearch.prototype.model = GiftModel;

      GiftSearch.prototype.ui = {
        search: "input[name=gift-search]"
      };

      GiftSearch.prototype.events = {
        'keyup input[name=gift-search]': "doSearch"
      };

      GiftSearch.prototype.doSearch = function(e) {
        if (this.ui.search.val().length >= 3) {
          console.log(this.ui.search.val());
          this.model.set("value", this.ui.search.val());
          if (this.model.save() !== false) {
            return console.log("saving");
          }
        }
      };

      return GiftSearch;

    })(Marionette.ItemView);
  }));

}).call(this);
