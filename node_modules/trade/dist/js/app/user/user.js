(function() {
  var __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

  define(["marionette", "user/userModel"], (function(Marionette, UserModel) {
    var User;
    return User = (function(_super) {
      __extends(User, _super);

      function User() {
        return User.__super__.constructor.apply(this, arguments);
      }

      User.prototype.template = "#user-view";

      User.prototype.model = UserModel;

      User.prototype.itemViewContainer = "user";

      User.prototype.events = {
        'click button#logout': "doLogout"
      };

      User.prototype.onBeforRender = function() {
        return this.model.set("status", true);
      };

      User.prototype.doLogout = function(e) {
        e.preventDefault();
        this.model.set("status", "inactive");
        if (this.model.save() === false) {
          return console.log(this.model.validationError);
        } else {
          return setTimeout(function() {
            return window.location.reload();
          }, 500);
        }
      };

      return User;

    })(Marionette.ItemView);
  }));

}).call(this);
